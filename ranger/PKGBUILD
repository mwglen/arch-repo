# Maintainer: Matt Glen <mwg2202@gmail.com>
pkgname=mwglen-ranger
pkgver=1
pkgrel=1
pkgdesc="A ranger configuration"
arch=('x86_64')
url=https://github.com/mwglen/arch-linux
license=('GPL')
md5sums=('SKIP')

makedepends=('rsync')
depends=('ranger' 'python-ueberzug-git')

package() { 
    rsync config $XDG_CONFIG_HOME
}
