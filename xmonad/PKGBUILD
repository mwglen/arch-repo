# Maintainer: Matt Glen <mwg2202@gmail.com>
pkgname=mwglen-xmonad
pkgver=1
pkgrel=1
pkgdesc="A xmonad configuration"
arch=('x86_64')
url=https://github.com/mwglen/arch-linux
license=('GPL')
md5sums=('SKIP')

makedepends=('rsync')
depends=(
    'xmonad' 'xmonad-contrib' 'devour'
    'mwglen-rofi'

    # Notifications
    'libnotify'
)

package() { rsync --append home $HOME }
